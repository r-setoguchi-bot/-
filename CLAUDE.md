# CLAUDE.md

このリポジトリで Claude Code が作業するときの共通ルール。

## プロジェクト概要

Google Apps Script プロジェクト（`コード.js` / `appsscript.json` / `.clasp.json`）。
デプロイは clasp 経由。

## 作業ルール

### 1. 長い資料・複数資料・動画・本の読み込みは NotebookLM 経由で処理する

以下に当てはまるものは、Claude Code が直接読み込まず、必ず NotebookLM
（`notebooklm-mcp-cli` の `nlm` コマンド / MCP サーバー）を経由して処理する。

- 長文の資料（PDF、議事録、仕様書、レポートなど）
- 複数の資料をまたいで横断的に読む必要があるもの
- 動画（YouTube など）
- 書籍・書籍相当の分量があるもの

手順:

1. 対象テーマのノートブックを用意する（無ければ `nlm notebook create "<テーマ名>"`）
2. 資料をソースとして追加する（`nlm source add <notebook> <URL またはファイル>`）
3. ノートブックに質問して回答を得る（`nlm chat` / `nlm notebook ask`）
4. **回答は必ず出典付きで受け取る。** 出典が付いていない回答はそのまま採用せず、
   出典を明示させたうえで受け取り直す。

要約や引用をユーザーに返すときも、どのソースの何ページ・どの箇所かを併記する。

### 2. 調べ物・分析の結果は毎回「リサーチ」フォルダに保存する

調査・リサーチ・分析を行ったら、**ユーザーに頼まれなくても毎回**、結果を
ファイルとして保存する。

- 保存先: リポジトリ直下の `リサーチ/` フォルダ
- ファイル名: `YYYY-MM-DD_<テーマ名>.md`（例: `2026-09-03_NotebookLM連携調査.md`）
- 同じ日に同じテーマで複数回行った場合は末尾に連番を付ける
  （例: `2026-09-03_競合調査_2.md`）

ファイルには最低限、次を含める。

- 調べた目的・問い
- 結論・要点
- 根拠と出典（NotebookLM 経由の場合はソース名と該当箇所）
- 実行日

保存したら、保存先のパスをユーザーに伝える。
